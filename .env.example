# Arena2API 环境变量配置
# 复制此文件为 .env 并修改配置

# ============ 服务器配置 ============

# 监听端口
PORT=9090

# ============ 认证配置 ============

# API Key逗号分隔，留空则不鉴权）
# 用户请求 /v1/chat/completions 时需要在 Authorization header 中携带
# 示例: sk-mykey1,sk-mykey2,sk-shared-key
API_KEYS=

# 扩展推送密钥（留空则不验证扩展身份）
# Firefox 扩展推送数据到服务器时需要携带此密钥
EXTENSION_SECRET=

# ============ Token 池配置 ============

# 每个 Profile 的 token 池上限
TOKEN_POOL_MAX=30

# Token 过期时间（毫秒）
# reCAPTCHA token 约 120 秒有效，设为 110000 留 10 秒余量
TOKEN_EXPIRY_MS=110000

# Profile 超时时间（秒）
# 超过此时间无推送则标记为不活跃，不再被分配请求
PROFILE_TIMEOUT_S=120

# ============ 调试配置 ============

# 启用调试日志（设为任意非空值启用）
DEBUG=

# 启用 prompt 调试（设为任意非空值启用）
PROMPT_DEBUG=